---
interface Props {
    title: string;
    body: string;
    icon: string;
}

const { title, body, icon } = Astro.props;
---

<div class="flex flex-col items-center justify-center lg:w-1/4 font-bold w-auto m-5 lg:mx-0 ">
    <h3 class="text-2xl text-center">{title}</h3>
    <i class=`${icon} my-5 text-8xl`></i>
    <p class="text-center">{body}</p>
</div>